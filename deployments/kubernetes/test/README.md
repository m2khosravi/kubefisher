# test workloads

- **`gpu-fake-workload.yaml`** — `pause` + `nvidia.com/gpu` for fast **cost/hour** annotation checks (no vLLM metrics).

Used by **`make cluster-install-test-gpu-fake`** / **`make cluster-e2e-cost-patcher-hour`**.
